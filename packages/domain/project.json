{
  "name": "domain",
  "root": "packages/domain",
  "sourceRoot": "packages/domain/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --filter domain build"
      }
    }
  }
}
